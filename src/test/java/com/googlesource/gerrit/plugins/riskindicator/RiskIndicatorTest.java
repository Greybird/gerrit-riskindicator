package com.googlesource.gerrit.plugins.riskindicator;

import static org.junit.Assert.assertTrue;

import org.junit.Test;

public class RiskIndicatorTest {
  @Test
  public void cookbookTest() {
    // Dummy test, only used to make sure the riskindicator plugin gets compiled
    // when running `buck test`, thus highlighting any compilation errors.
    assertTrue(true);
  }
}
